# Desenvolvimento local: não precisa definir nada. O Vite já faz proxy de /api
# para http://localhost:8080 (ver vite.config.js).

# Produção (Vercel): defina esta variável nas Environment Variables do projeto
# com a URL pública do backend no Render. Sem a barra final.
VITE_API_BASE_URL=https://seu-backend.onrender.com
